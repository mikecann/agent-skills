# 2026-03-27 - 08 Mixed React

## Fixture

- `evals/deslop/fixtures/08-mixed-react`

## Result

- changed: `yes`

## Output Summary

The eval:

- added an early return for `!user`
- removed the redundant `open` alias
- replaced `&&` rendering with `? : null`
- removed `user!` by narrowing
- correctly kept the shared `handleRetry` hoisted because it is reused in two
  places

Output excerpt:

```tsx
export function UserPanel({ user, hasError, isSaving }: Props) {
  if (!user) return <div>No user</div>;

  const handleRetry = () => {
    saveDraft({ userName: user.name })
      .then(() => toast.success("Saved"))
      .catch(() => toast.error("Failed"));
  };

  return (
    <div>
      {hasError ? <ErrorBanner onRetry={handleRetry} /> : null}
      <div>{user.name}</div>
      <button onClick={handleRetry} disabled={isSaving}>
        Retry
      </button>
    </div>
  );
}
```

## Rubric

- `behavior_preserved`: `pass`
- `api_preserved`: `pass`
- `slop_reduced`: `pass`
- `style_rule_following`: `pass`
- `unnecessary_churn`: `pass`

## Notes

- This is one of the more useful fixtures in the suite.
- It proves the agent can avoid applying the inline-handler rule mechanically.
- Reuse won over inlining here, which is the right call.
